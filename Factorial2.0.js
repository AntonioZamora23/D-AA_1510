// Funcion recursiva para calcular el factorial de un numero 'n'function factorial(n) {  // Verifica si el numero es negativo  if (n < 0) {    return -1; // Si el numero es negativo, devuelve -1 para indicar un error  } else if (n === 0) {    return 1; // El factorial de 0 es 1  } else {    // Caso recursivo: multiplica 'n' por el factorial de 'n-1' (llamada recursiva)    return n * factorial(n - 1);  }}const numero = 5; // Cambia el valor de 'numero' segun el numero del que deseas calcular el factorialconst resultado = factorial(numero); // Llamamos a la funcion para calcular el factorialconsole.log(`El factorial de ${numero} es: ${resultado}`); // Mostramos el resultado en la consola